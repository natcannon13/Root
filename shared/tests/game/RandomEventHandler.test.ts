import { describe, test } from "vitest";


describe("RandomEventHandler", () => {
    describe("pick", () => {
        test("returns one of the options provided", () => {});
        test("returns each option with equal probability over multiple calls", () => {});
    });

    describe("pickX", () => {
        test("returns the correct number of options", () => {});
        test("returns options only from the provided list", () => {});
        test("returns unique options", () => {});
        test("returns each option with equal probability over multiple calls", () => {});
    });

    describe("yesno", () => {
        test("returns a boolean", () => {});
        test("returns true and false with equal probability over multiple calls", () => {});
    });

    describe("pickRange", () => {
        test("returns a number within the provided range", () => {});
        test("returns each number with equal probability over multiple calls", () => {});
    });

    describe("pickOrder", () => {
        test("returns a valid order of the provided options", () => {});
        test("returns each possible order with equal probability over multiple calls", () => {});
    });
});