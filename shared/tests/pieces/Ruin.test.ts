import { describe, test } from "vitest";

describe("Ruin", () => {
    test("initializes with given id and items", () => {});
    test("removeItem removes and returns the specified item", () => {});
    test("removeItem throws if the item is not found", () => { });
});